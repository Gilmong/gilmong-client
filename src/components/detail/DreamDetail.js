import React from "react";
import styled from "styled-components";

const DreamDetail = () => {
  return <DreamDetailWrapper></DreamDetailWrapper>;
};

export default DreamDetail;
const DreamDetailWrapper = styled.div``;
